// Copy this file to `config.ts` and fill in your own keys.
// `config.ts` is git-ignored so your secrets never get committed.

export const CONFIG = {
  supabaseUrl:     "https://YOUR-PROJECT.supabase.co",
  supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY",
  apiBaseUrl:      "https://your-backend.vercel.app",
  owmApiKey:       "YOUR_OPENWEATHERMAP_API_KEY",
};
